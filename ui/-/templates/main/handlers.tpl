<div class="{__NODE_ID__}">

    <div class="handlers">
        <!-- handler -->
        <div class="handler {SELECTED_CLASS}" draggable="handler" handler_id="{ID}" hover="hover">
            <div class="buttons" hover="hover">
                {RENAME_BUTTON}
                {DUPLICATE_BUTTON}
                {DELETE_BUTTON}
            </div>
            <div class="name">
                {NAME}
            </div>
            <div class="cb"></div>
        </div>
        <!-- / -->
    </div>

    {CREATE_BUTTON}
    <div class="cb"></div>

</div>
