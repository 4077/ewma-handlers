<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="bar" hover="hover" node_id="{NODE_ID}">
        {CREATE_HANDLER_BUTTON}
        {CREATE_CALL_BUTTON}
        {CREATE_VALUE_BUTTON}
        {CREATE_DATA_MODIFIER_BUTTON}

        <div class="info">
            <!-- combine_mode -->
            <div class="combine_mode">{VALUE}</div>
            <!-- / -->
            <div class="cb"></div>
        </div>
        <div class="cb"></div>

        <div class="cp hidden" node_id="{NODE_ID}">{CP}</div>
    </div>

    <div class="nodes">
        {NODES}
    </div>

</div>
