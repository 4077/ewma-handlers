<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="items">
        <!-- item -->
        <div class="item" item_id="{ID}">
            {CONTENT}
        </div>
        <!-- / -->
    </div>

    <div class="buttons">
        <!-- create_button -->
        {CONTENT}
        <!-- / -->
    </div>

    {DIALOGS}

</div>
